# 供应商风险分析 Agent 演示脚本

约 8 分钟。

## 提前准备

```bash
# 终端1：后端
cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000

# 终端2：前端
cd frontend && npx vite --host 0.0.0.0 --port 5173
```

打开浏览器 `http://localhost:5173`

---

## 一、风险看板（30 秒）

打开页面 → 默认在「风险看板」标签。

**展示要点：**
- 4 张概览卡片：监控 N 家、告警 N 条、高风险 N、低风险 N
- 渐变色风险分布条，绿/黄/红 一目了然
- 下方企业详情表，按风险从高到低排列

> 话术："这里是实时风险看板，一眼看清所有供应商的整体健康状况。"

---

## 二、智能对话（核心 4 分钟）

切到「智能对话」标签页。

### 2.1 单公司分析

```
帮我分析海康威视的风险
```

Agent 自动：搜索全名 → 评估 → 返回报告

> 话术："注意，这不是简单地查数据库。Agent 自主决定了调用搜索工具找到全称，再调用评估工具获取数据，最后生成分析。"

### 2.2 评分解释

```
为什么它是 54 分
```

Agent 逐项列出：财务 8 分（现金流为负）、司法 27 分（诉讼+担保+质押）、经营 19 分（破产+环保+法人变更）

> 话术："每一项评分都能追溯到具体数据，不是黑盒。"

### 2.3 多公司对比

```
对比一下海康和宝钢
```

Agent 自动搜索两家公司 → 分别评估 → 生成对比分析。

> 话术："它能自动拆解任务，先搜 A、再搜 B、分别评估，最后对比。这是 ReAct 循环在起作用。"

### 2.4 批量操作

```
把海康和宝钢加入监控
```

> 话术："用自然语言就能管理监控清单。看左侧，清单会实时更新。"

---

## 三、风险评估（1 分钟）

切到「风险评估」标签，输入「腾讯」

**展示要点：**
- 彩色评分圆环 + 进度条
- 四列财务指标（营收增长 9%、净利增长 22%、现金流 11 元/股）
- 四列风险矩阵：司法 / 经营 / 资金链 / 财务
- 对外担保、股权质押、破产清算、环保处罚等细分维度
- 港股数据自动获取（A 股 + 港股都支持）

> 话术："腾讯是港股，我们没有它的股票代码，但系统通过名称匹配到了港交所代码 00700，自动从 AkShare 拉到了财报。"

---

## 四、侧边栏交互（30 秒）

- 点击清单中任意公司 → 自动跳转到风险评估页展示该公司的完整报告
- 输入名称加入监控
- 勾选 + 移除

> 话术："点击清单里的公司就能快速查看风险详情，不用复制粘贴公司名。"

---

## 五、告警中心（30 秒）

切到「告警中心」标签。

**展示要点：**
- 时间线卡片：黄色警告 / 红色严重
- 显示变化内容："诉讼数量 38→40"、"被执行记录 0→2"
- "清空记录"按钮

> 话术："每次查询或定时巡检都会自动保存快照。下次再查时对比变化，有新增风险就自动告警。"

---

## 六、技术亮点总结

| 亮点 | 说明 |
|------|------|
| ReAct Agent | LLM 自主思考-行动-观察循环，不是固定流水线 |
| 多维评分 | 13 个指标，连续函数，不是二元的"有/没有" |
| 双数据源 | A 股 + 港股财报（AkShare 免费），工商+司法（天眼查） |
| 成本控制 | 免费/付费双通道巡检，日常不花钱 |
| 持续监控 | 定时快照对比，风险变化自动告警 |

---

## 演示数据（可选）

想演示告警功能，先跑一次巡检造几条数据：

```bash
curl -X POST http://localhost:8000/alert/check-all
```

或者手动对几家公司连续评估两次来触发告警：

```bash
curl -X POST http://localhost:8000/risk/assess \
  -H "Content-Type: application/json" \
  -d '{"company_name":"杭州海康威视数字技术股份有限公司"}'
# 等几秒再跑一次
```

---

## 高频问题准备

**Q: 它和 ChatGPT 有什么区别？**
A: ChatGPT 凭记忆回答，会编造数据。它必须调工具获取真实数据后才回答，评分公式是代码算的，不靠 LLM 猜测。

**Q: 数据从哪里来？**
A: 企业工商和司法数据来自天眼查 API，上市公司财报来自 AkShare（免费），港股也能查。

**Q: 天眼查是按次收费的，成本怎么控制？**
A: 数据缓存在本地 MongoDB，只在新公司首次查询时调天眼查。定时巡检也分免费通道（AkShare 财报）和付费通道（天眼查全量刷新），分别配置频率。
