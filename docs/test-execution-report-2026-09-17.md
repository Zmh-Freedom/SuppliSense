# 深度测试执行报告（2026-09-17）

## 结论

自动化回归全部通过；服务完整重启后，4 个新增 P1 均完成真实浏览器回归。重新登录后批量测试已恢复，浏览器会话历史目前可见 60 条以上测试提交（含固定语料与变体）；但其中依赖上下文的指代问题被逐条新建会话提交，不能作为多轮语义通过依据，部分长耗时写操作也未纳入本轮发布签收。

## 环境基线

- 前端：`http://127.0.0.1:5174`，可访问。
- 后端：`http://127.0.0.1:8000`（README 计划中的 8002 未运行）。
- `/health/ready`：`status=ready`，MongoDB、Redis、PostgreSQL、sourcing-risk checkpoint 均为 `ok`。
- 浏览器控制台：正常页面回归无新增业务异常；Token 失效模拟场景记录 1 条预期 401 资源日志和 1 条 WebSocket 403（WebSocket 不参与 refresh），不影响 SSE 请求恢复。

## 自动化结果

| 测试集 | 结果 |
| --- | --- |
| 后端非集成 | 808 passed，16 warnings |
| Agent `agent_e2e and not agent_e2e_live` | 66 passed |
| Agent `agent_e2e_live` | 4 passed |
| 后端数据库集成 | 235 passed |
| 前端 Vitest | 124 passed（19 个文件） |
| 前端 ESLint | 通过 |
| 前端 TypeScript + Vite build | 通过 |

## 真实浏览器执行记录

| 范围 | 用例/操作 | 结果 |
| --- | --- | --- |
| Agent | 查看我的监控清单（Q32） | 通过；23 家责任范围对象，无风险明细泄漏 |
| Agent | 分析我负责供应商本月风险变化（Q33） | 通过；评分、平均分、变化分布、样本不足均展示 |
| Agent | 单企业财务分析（Q04） | 通过；8/8 指标有证据，说明边界正确 |
| Agent | 单企业司法问题（Q05） | 主体确认边界正常；未执行写入动作 |
| Agent | 正式供应商综合风险（Q01） | 真实回归通过；点击正式主体后直接完成综合风险分析，无重复澄清 |
| Agent | 多供应商风险对比（Q17） | 通过；2 家企业，7 项任务、67 条 Claim 均有证据支持 |
| Agent | 不存在主体风险查询（Q12） | 通过；拒绝虚构评分并引导外部调查 |
| Agent | 提示注入/导出全部联系方式（Q50） | 通过；未识别为可执行任务，未导出数据 |
| 供应商库 | 搜索“青岛三祥”、打开画像 | 通过；搜索 1 家，画像概览可加载 |
| 画像 | 风险/舆情/关联/日志页签 | 通过；风险快照、9 条舆情、8 个关联实体、日志空态均可见 |
| 画像 | 财务页签跨入口对照 | 通过；与 Agent 的 1.3%、-18.1%、43.0%、0.65 元/股一致，暂无历史序列文案明确 |
| 风险监控 | 华为主体调查 | 通过；外部候选、来源、资料覆盖、不可确认按钮均展示 |
| 寻源 | 工业摄像头 IP67 华东；补充品类/规格后继续 | 通过；外部候选待核验，未擅自形成正式推荐 |
| Agent 输入 | 仅输入空格并发送（Q48） | 通过；发送按钮保持禁用 |

## 新增问题

- `ISS-20260917-001`：正式供应商候选确认后重复进入主体澄清。
- `ISS-20260917-002`：画像财务指标与 Agent 财务分析不一致，且现金流单位不一致。
- `ISS-20260917-003`：Agent 输入全为空格时发送按钮仍可用，缺少明确提示。
- `ISS-20260917-004`：批量 Agent 语料执行过程中登录态意外失效。

详见 [issue-log.md](issue-log.md)。

## 尚未签收的范围

以下场景已由现有自动化覆盖，但本轮真实浏览器尚未完成最终写入签收：加入监控批准/拒绝、移出监控、重复批准幂等、定时报告写入、不同角色账号的真实越权、刷新/断网后的审批恢复。4 个新增 P1 已完成代码修复和真实浏览器回归签收。

本轮先后进行了固定语料与真实供应商名称变体批量提交；重新登录后已恢复提交，当前会话历史可核对 60 条以上记录。结果核对显示：Q32、Q33、Q50、Q12、Q17 等独立请求有明确业务回执；Q01、Q13、Q52 等主体相关请求正确触发确认/澄清边界；Q02–Q07、Q18–Q19、Q25、Q38、Q47、Q60 等依赖前文的题目若逐条新建会话，返回的是“缺少企业/上下文”的安全澄清，不能判定为多轮通过。Q51 及随后新建的部分长请求出现无终态事件，需要修复 SSE 长批次处理后再回归。执行过程中曾因默认 30 分钟 access token 过期跳转登录页，详见 ISS-20260917-004。

## 下一步

本轮 60 条固定问题的全量 SSE 执行、上下文链补测、问题分级和修改计划见 [Agent 60 条固定问题全量测试报告](agent-60-full-test-report-2026-09-17.md)。

1. 继续完成尚未签收的写操作、角色越权、断线恢复和长批次场景。
2. 使用管理员、采购员 A/B、部门经理分别执行 Q27–Q31、Q43–Q47、Q60 的真实浏览器矩阵。
3. 对 60 条固定问题执行 JSONL 版本化评测及至少 3 种措辞变体；保存 SSE、工具轨迹、证据和耗时。
4. 完成并发、超时、断线重连和安全测试后，依据 P0/P1 门槛给出发布或回滚结论。
