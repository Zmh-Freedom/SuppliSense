# Agent 60 条固定问题全量测试报告（2026-09-17）

## 结论

60 条固定问题已全部执行，且每条请求都返回 HTTP 200 和 SSE 终态；本轮没有传输错误、HTTP 错误或无终态事件。但“执行完成”不等于“业务验收通过”：占位测试主体未配置、依赖上下文的问题曾被错误地放在独立会话、以及多轮/写操作路由存在缺陷，因此当前不能判定 60 条全部通过。

本轮分两层执行：

1. 60 条逐题独立会话基线：验证请求可达性、终态事件、澄清安全边界和基础路由。
2. 26 条上下文链补测：使用同一 session 验证 Q01→Q02–Q08、Q17→Q18–Q19、Q20→Q21、Q22→Q23、Q24→Q26、Q27→Q31、Q46→Q47/Q60 等多轮语义。

## 执行结果

| 执行层 | 数量 | 结果 |
| --- | ---: | --- |
| 独立会话固定问题 | 60 | 60/60 有终态；33 条 completed，27 条 clarification |
| 上下文链补测 | 26 | 18 条 completed，8 条 clarification；其中多条为缺陷复现而非通过 |
| 传输稳定性 | 60 | 0 HTTP 错误、0 transport error、0 no-terminal-event |
| 既有自动化门禁 | — | 后端非集成 808 passed；Agent E2E 66 passed；前端 124 passed；lint/build 通过 |

独立会话完成终态的题目：Q11、Q15、Q16、Q20–Q34、Q37、Q42–Q52、Q55、Q56、Q58。

独立会话进入澄清的题目：Q01–Q10、Q12–Q14、Q17–Q19、Q35、Q36、Q38–Q41、Q53、Q54、Q57、Q59、Q60。对于需要前置主体或前文指代的题目，独立会话澄清本身是安全行为，不能作为多轮通过依据。

## 主要问题与修改计划

| 问题 | 影响题目 | 优先级 | 修改方向 |
| --- | --- | --- | --- |
| 固定语料占位主体未配置 | Q01–Q60 多个主体/审批题 | P0（测试阻断） | 建立 E2E_TEST 主体、外部候选、冲突/过期快照、暂停任务和可清理审批数据；启动前做 fixture 完整性校验 |
| 多轮主体确认重复澄清 | Q01→Q02–Q08、Q13→Q14 | P0 | 统一 NFKC/空白/口语前缀归一化；确认后持久化 canonical supplier/company/monitor ID；后续轮次优先消费已确认实体 |
| 后续约束和候选序号未消费 | Q22→Q23、Q24→Q26 | P1 | 扩展 current_task 保存预算、替代、候选版本和 candidate_id；增加“第 N 家”和“没有合适”确定性路由 |
| 移出/批量加入监控意图误路由 | Q30、Q51 | P0 | 补齐 add/remove/batch intent；写意图识别失败时安全澄清，禁止回退到 get_watchlist 或风险分析；批量动作必须生成 proposal |
| 定时报告“给…设置”触发主体澄清 | Q35 | P1 | 解析“给/为/替…设置报告”的目标、频率和报告类型，直接进入审批状态机；补充重复创建/取消幂等 |
| 取消/恢复无可验证任务前置态 | Q46、Q47、Q60 | P0（验收阻断） | 提供 paused/approval-required fixture；覆盖 stop、refresh replay、resume、denied/completed；无任务时返回结构化状态 |

对应问题已先登记在 [issue-log.md](issue-log.md) 的 ISS-20260917-005 至 ISS-20260917-010，状态均为“待修复”。

## 建议实施顺序

### 第一阶段：恢复可重复测试（P0）

1. 建立固定测试数据映射和隔离数据库快照，确保每个 Q 编号都有主体、证据和权限前置条件。
2. 修复统一实体归一化和多轮确认状态消费。
3. 修复 add/remove/batch 写意图路由，增加“不能隐式执行写入”的断言。
4. 建立可暂停、可恢复、可取消的 durable run fixture。

### 第二阶段：修复多轮业务语义（P1）

1. 扩展寻源任务状态和候选选择协议。
2. 补齐预算、替代、序号、无候选等追问解析。
3. 完成定时报告创建、更新、取消和重复操作幂等。

### 第三阶段：回归和签收

1. 重新执行 60 条固定问题，每题至少 3 种措辞变体。
2. 对依赖上下文的题目使用预定义 session script，不得逐条新建会话。
3. 对 P0/P1 题目保留 SSE、工具轨迹、证据引用、审批 ID、耗时和浏览器截图。
4. 使用 Codex 内部浏览器或现有 Chrome 的新标签页完成 UI 抽样回归；验证后关闭新增标签页/会话。
5. 只有在 P0 100% 通过、P1 无阻断且写操作/权限/恢复闭环完成后，才将问题状态改为“已修复已回归”。

## 当前签收结论

本轮可以签收“60 条请求均可达且 SSE 有终态”，不能签收“60 条业务语义全部通过”。下一步应先完成 ISS-20260917-005、006、008、010，再回归 ISS-20260917-007、009，最后重跑全量 60 条及三种变体。
