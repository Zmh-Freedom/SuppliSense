# 比赛演示检查清单

## 启动检查

1. 启动后端和前端。
2. 打开 `http://127.0.0.1:5174/login`。
3. 确认后端 `http://127.0.0.1:8000/health/ready` 返回 `status=ready`，且 MongoDB、Redis、PostgreSQL、Harness checkpointer 均为 `ok`。

## 主流程

1. 使用管理员账号登录，进入 AI 工作台。
2. 输入“复核青岛三祥科技股份有限公司”。
3. 展示上市公司盈利变化、交易连续性、证据表和趋势图。
4. 输入“复核上海汽车制动系统有限公司”。
5. 展示结算环比变化、财务信息覆盖边界和采购复核事项。
6. 展开执行详情，展示任务、审批、执行结果和证据回写状态。

## 权限与边界

1. 使用采购员账号登录，确认只能看到责任范围内的供应商。
2. 输入其他采购员负责的正式供应商名称，确认 Agent 返回无权访问且不泄露明细。
3. 输入不在供应商库的企业名称，确认系统说明无法按正式供应商复核；如用户要求评估外部企业，再展示主体检索和待验证数据边界。

## 寻源流程

1. 输入“帮我为后轮制动鼓寻找历史供应商和外部候选”。
2. 展示历史合作供应商、盖世外部候选和“外部待验证候选”标识。
3. 对外部候选执行主体核验，确认未核验主体不会直接进入正式供应商目录。

## 讲解口径

- 收货记录数表示源明细行数，不等于零件数量或交付能力。
- 结算波动是复核信号，不单独等同于供应中断或经营风险。
- 没有财报的非上市供应商只展示数据覆盖边界，不生成虚假财务结论。
- Agent 的结论始终展示证据来源、可信度和采购人员下一步动作。

## 2026-09-13 演示数据冻结与核心流程验收

本轮使用管理员账号，通过 `http://127.0.0.1:8002/api/v1/chat/stream` 实际调用活动 Harness；`DEMO_DATA_FREEZE=true`，当前飞书正式供应商快照固定为 20 家，供应商库与总览接口均返回 20 家。

| 固定问题 | 结果 | 实际表现 |
| --- | --- | --- |
| 查看监控清单 | 通过 | 返回 20 家当前责任范围内供应商，并列出名称和稳定对象引用。 |
| 分析我负责的供应商本月风险变化 | 通过（有数据边界） | 返回 20 家变化状态；青岛三祥有 4 个数据点，其余多数只有 1 个数据点并明确标记“数据不足”。 |
| 复核青岛三祥科技股份有限公司 | 部分通过 | 返回 93/100 安全评分、财务指标、司法风险和交易连续性；主回答仍混入 `risk_detail.*` 与 `partial` 内部字段，关联 ISS-20260913-004。 |
| 帮我为后轮制动鼓寻找历史供应商和外部候选 | 通过 | 返回 14 家候选，其中 4 家历史合作、10 家盖世外部待核验候选，来源和待核验状态均已标识。 |

补充验证：输入“把上海海拉电子有限公司加入监控清单”未进入人工审批，而是返回泛化风险结论，关联 ISS-20260913-003；该问题修复前不应作为比赛写操作主流程演示。
