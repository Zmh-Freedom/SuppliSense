# 基于 ReAct Agent 的供应商风险智能分析系统

## 一、课题背景

在企业采购管理场景中，供应商风险判断长期依赖人工经验。采购人员需要手动查询企业工商、司法、财报等多源数据，主观判断商合作风险。这一过程存在三个痛点：

1. **效率低**：手动查询多个平台，单个供应商平均耗时 30 分钟以上
2. **信息不全**：容易遗漏关键风险维度，如对外担保、股权质押等间接风险信号
3. **标准不一**：不同采购人员的风险判断标准差异大，缺乏量化依据

针对上述问题，本课题构建了一套基于 ReAct Agent 架构的供应商风险智能分析系统，实现企业风险数据的自动聚合、量化评分和自然语言交互。

## 二、系统架构

系统采用前后端分离架构，后端基于 FastAPI 框架，前端基于 React + TypeScript，AI 引擎采用 DeepSeek 大语言模型。

```
用户（React 前端 / API 调用）
  ↓
FastAPI 后端
  ├── ReAct Agent（DeepSeek LLM）
  │     ├── 思考 → 选择工具 → 执行 → 观察 → 再思考
  │     └── 6 个工具：搜索 / 评估 / 预警 / 监控 / 增删 / 清单
  ├── 数据层
  │     ├── MongoDB（企业数据缓存、快照、告警）
  │     ├── 天眼查 API（工商 + 司法数据，付费）
  │     └── AkShare（财报数据，免费）
  └── 定时任务（APScheduler）
        ├── 每日 9:00 免费巡检（AkShare 财报）
        └── 每周一 9:00 付费巡检（天眼查全量刷新）
```

## 三、核心功能

### 1. 智能风险对话

用户通过自然语言与系统交互，Agent 自主完成工具调用链：

- 单公司分析：「分析海康威视的风险」
- 多公司对比：「对比海康和宝钢」
- 评分解释：「为什么是 54 分」
- 批量操作：「把这两家加入监控清单」

Agent 采用 ReAct（Reasoning + Acting）循环架构，每一步都遵循「思考→行动→观察」模式，自主决定何时调用哪个工具、调用几次、何时给出最终回答。对话历史持久化存储，支持跨轮次上下文记忆。

### 2. 多维度风险评分

评分模型覆盖 13 个风险指标，分三类加权计算（总分 0-100）：

| 类别 | 指标 | 计算方式 |
|------|------|---------|
| 财务风险 | 资产负债率、现金流、营收增长、净利增长 | 连续函数，按程度计分 |
| 司法风险 | 诉讼、被执行、失信、重大诉讼、对外担保、股权质押 | 按数量线性，设上限 |
| 经营风险 | 经营异常、行政处罚、法人变更、破产清算、环保处罚 | 按次数计分 |

与传统二元判断（有风险/无风险）不同，本系统采用连续函数计算。例如，1 起诉讼 +0.3 分，40 起诉讼封顶 10 分，避免单一指标主导总分。每次评估自动保存快照，支持历史趋势对比。

### 3. 自动数据获取

- **A 股上市公司**：通过 AkShare 自动获取营收增长率、净利增长率、资产负债率、每股经营现金流
- **港股上市公司**：通过名称匹配港交所代码，调用 AkShare 港股接口获取同比数据
- **非上市公司**：自动跳过财报获取，仅基于工商和司法数据评分
- **缓存策略**：财报数据 24 小时自动过期，避免重复请求

### 4. 持续预警监控

- **快照对比**：每次评估自动保存快照，后续查询时对比变化
- **双通道巡检**：免费通道（每日 AkShare 财报）+ 付费通道（每周天眼查全量刷新）
- **变化检测**：诉讼新增、被执行新增、失信新增等自动生成告警
- **前端看板**：风险分布图、企业详情列表、告警时间线卡片

## 四、技术亮点

| 亮点 | 说明 |
|------|------|
| ReAct Agent 架构 | LLM 自主决策工具调用序列，非固定流程 |
| 连续风险评分 | 13 个指标按程度计分，替代二元的"有/没有" |
| 多数据源融合 | 天眼查（工商司法）+ AkShare（A 股港股） |
| 成本可控 | 免费/付费双通道，本地缓存减少 API 调用 |
| 自然语言交互 | 支持单公司、多公司对比、追问、批量操作 |
| 持续监控 | 定时快照对比，风险变化自动告警 |

## 五、应用场景

1. **供应商准入审核**：招标前批量评估候选供应商风险等级
2. **日常风险监控**：定时巡检现有供应商，及时发现风险变化
3. **采购决策支持**：多供应商风险对比，辅助采购决策
4. **合规审计**：系统化记录供应商风险评估历史

## 六、技术栈

| 层 | 技术 |
|----|------|
| 后端框架 | FastAPI + Pydantic |
| AI 引擎 | DeepSeek（OpenAI 兼容 API） + ReAct Agent |
| 数据库 | MongoDB |
| 定时任务 | APScheduler |
| 财报数据 | AkShare |
| 前端 | Vite + React 19 + TypeScript + Tailwind CSS |
| 测试 | pytest（16 个单元测试） |
